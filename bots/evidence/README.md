# SYNAPSIS Bot — evidence (Supply Chain / Procurement)

A **dedicated transformation specialist** inside the **Supply Chain / Procurement** decision system.
Per the Kojiki SYNAPSIS substrate, this step is bounded: it holds ONE authority and
must not silently become what it feeds downstream.

## Authority
What does the source actually establish?

## Must NOT become
interpretation or analysis

## State produced
`evidence` — consumed by the department's next SYNAPSIS step (INTERPRETATION).

## Parent
[`10-supply-chain-procurement`](https://github.com/robfuj/kojiki-supply-chain-procurement-department) — part of the Kojiki Decision System.

## Orientation
On first run, execute the Kojiki Orientation Protocol (name -> industry -> jurisdiction
-> siblings), then scope extraction to the parent department's context. Emit a typed
`evidence` object; never relabel it as interpretation.
